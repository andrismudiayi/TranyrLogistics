@using TranyrLogistics.Views.Helpers;

@model IEnumerable<TranyrLogistics.Models.ShippingTerms>

@{
    ViewBag.Title = "Shipping Terms";
}

<h3>@ViewBag.Title</h3>
<br />

<p>
    @Html.SecureActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Standard)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Standard)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.SecureActionImageLink(@Url.Content("~/Images/application_edit.png"), "Edit", "Edit", "ShippingTerms", new { id=item.ID }, new Dictionary<string, string> { { "title", "Edit" } }, null)
            @Html.SecureActionImageLink(@Url.Content("~/Images/application_view_detail.png"), "Details", "Details", "ShippingTerms", new { id=item.ID }, new Dictionary<string, string> { { "title", "Details" } }, null)
            @Html.SecureActionImageLink(@Url.Content("~/Images/application_delete.png"), "Delete", "Delete", "ShippingTerms", new { id=item.ID }, new Dictionary<string, string> { { "title", "Delete" } }, null)

        </td>
    </tr>
}

</table>
